fastapi>=0.110,<0.120
uvicorn[standard]>=0.29
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.6
pydantic-settings>=2.2
qdrant-client>=1.9
redis>=5.0
httpx>=0.27
python-multipart>=0.0.18
# Auth (Feature 3, pinned now to keep foundation stable)
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# Pinned: passlib 1.7.4 crashes with bcrypt>=5 (72-byte wrap detection). 4.x works.
bcrypt==4.0.1
email-validator>=2.0
# Ingestion (Feature 4)
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
huggingface_hub>=0.23
# Search intelligence (Phase 2)
fastembed>=0.3
# Agents + observability (Phase 4)
langgraph>=1.0
langsmith>=0.3
# Pinned: uuid-utils>=0.13 binaries are blocked by this machine's App Control policy
uuid-utils==0.12.0
# Security (Phase 3)
presidio-anonymizer>=2.2
phonenumbers>=9.0
# Tests
pytest>=8.0
pytest-asyncio>=0.23
